env_key: PSAP_FORGE_FOURNOS_DEPLOY_SECRET_PATH
description: |
  Vault to access FOURNOS Kube API in the PSAP-mgmt cluster

content:
  kubeconfig:
    description: |
      KUBECONFIG file to access the PSAP-mgmt cluster
